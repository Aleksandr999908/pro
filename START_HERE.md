# 🚀 Инструкция по запуску проекта

## Шаг 1: Установка зависимостей

Откройте командную строку (Terminal/PowerShell) в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

**Если pip не найден, попробуйте:**
```bash
python -m pip install -r requirements.txt
```

**Или установите основные зависимости вручную:**
```bash
pip install torch torchvision pillow opencv-python imageio imageio-ffmpeg tqdm pyyaml numpy pandas
```

## Шаг 2: Обработка видео

### Вариант А: Автоматический запуск (Windows)

Просто запустите файл:
```
scripts\process_videos.bat
```

### Вариант Б: Запуск через Python

```bash
python scripts/extract_frames.py --video_dir data/videos --output_dir data/frames --fps 1.0 --roi_output data/roi --preserve_structure --process_images
```

### Вариант В: Минимальная команда (использует значения по умолчанию)

```bash
python scripts/extract_frames.py
```

## Шаг 3: Проверка результатов

После выполнения скрипта проверьте, что созданы папки:
- `data/frames/` - извлеченные кадры
- `data/roi/` - ROI кадры (пока копии, для дальнейшей обработки)

## Шаг 4: Обучение модели (опционально)

После обработки видео можно обучить модель:

```bash
python scripts/train.py --config models/config.yaml --data_dir data/roi --output_dir models/checkpoints
```

## Шаг 5: Запуск API сервиса (опционально)

```bash
python service/app.py
```

API будет доступен по адресу: http://localhost:8000

## ⚠️ Возможные проблемы

### Проблема: "ModuleNotFoundError: No module named 'imageio'"

**Решение:** Установите зависимости:
```bash
pip install imageio imageio-ffmpeg
```

### Проблема: "No module named 'cv2'"

**Решение:**
```bash
pip install opencv-python
```

### Проблема: Ошибки при обработке видео

**Решение:** Убедитесь, что установлен ffmpeg:
- Windows: https://www.ffmpeg.org/download.html
- Или через imageio-ffmpeg: `pip install imageio-ffmpeg`

### Проблема: Медленная обработка

**Решение:** Обработка видео может занять время. Для тестирования можно:
- Использовать `--fps 0.5` для извлечения реже кадров
- Обработать только один месяц/папку для начала

## 📝 Структура данных после обработки

После выполнения скрипта будет создана структура:

```
data/
├── frames/
│   └── 01.2025/
│       ├── Волк/
│       │   └── 1/
│       │       ├── frame_000000.jpg
│       │       └── ...
│       └── Лисица/
│           └── ...
└── roi/
    └── (та же структура)
```

## 🎯 Быстрый старт (одной командой)

Если все зависимости установлены, можно запустить всё одной командой:

```bash
python scripts/extract_frames.py && echo "Обработка завершена!"
```

## 📚 Дополнительная информация

- Полная документация: `README.md`
- Примеры использования: `EXAMPLE_USAGE.md`
- Быстрый старт: `QUICKSTART.md`
