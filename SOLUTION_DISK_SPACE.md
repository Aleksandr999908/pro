# 🔴 Решение проблемы: Недостаточно места на диске

## Проблема

Во время обработки видео возникла ошибка:
```
OSError: [WinError 112] Недостаточно места на диске
```

**Причина:** Диск заполнен, скрипт успел обработать ~64 видео, но места больше нет.

## ✅ Решения

### Вариант 1: Освободить место на диске

1. **Удалите временные файлы:**
   ```bash
   # Очистка временных файлов Windows
   %TEMP%
   # Или через PowerShell:
   Remove-Item $env:TEMP\* -Recurse -Force -ErrorAction SilentlyContinue
   ```

2. **Очистите корзину**

3. **Освободите место в OneDrive** (если синхронизация включена)

### Вариант 2: Сохранять кадры на другой диск

Если у вас есть другой диск (например, D:\), используйте его:

```bash
python scripts/extract_frames.py \
    --video_dir data/videos \
    --output_dir D:/frames \
    --fps 1.0 \
    --roi_output D:/roi \
    --preserve_structure \
    --process_images
```

### Вариант 3: Обрабатывать видео частями

Обработайте только несколько месяцев за раз:

```bash
# Обработать только один месяц
# Скопируйте нужную папку (например, 01.2025) во временную папку
# И обработайте её

python scripts/extract_frames.py \
    --video_dir data/videos/лисы-волки/01.2025 \
    --output_dir data/frames/01.2025 \
    --fps 0.5 \
    --roi_output data/roi/01.2025 \
    --preserve_structure \
    --process_images
```

### Вариант 4: Уменьшить частоту извлечения кадров

Используйте меньше кадров в секунду (меньше кадров = меньше места):

```bash
python scripts/extract_frames.py \
    --video_dir data/videos \
    --output_dir data/frames \
    --fps 0.5 \
    --roi_output data/roi \
    --preserve_structure \
    --process_images
```

**Параметр `--fps 0.5`** означает извлечение 1 кадра каждые 2 секунды (вместо каждого кадра).

### Вариант 5: Продолжить обработку с места остановки

Если уже обработано 64 видео, можно продолжить с оставшимися:

1. **Проверьте, какие видео уже обработаны:**
   ```bash
   # Посмотрите структуру data/frames
   ls data/frames
   ```

2. **Создайте список уже обработанных видео**

3. **Обработайте только те видео, которых нет в data/frames**

## 📊 Проверка занятого места

### Windows (PowerShell):
```powershell
Get-ChildItem -Path data/frames -Recurse -File | Measure-Object -Property Length -Sum | Select-Object @{Name="Size(GB)";Expression={[math]::Round($_.Sum / 1GB, 2)}}
```

### Проверка свободного места:
```powershell
Get-PSDrive C | Select-Object Used,Free
```

## 💡 Рекомендации

1. **Обрабатывайте постепенно:**
   - Сначала 1-2 месяца
   - Проверьте результаты
   - Освободите место или перейдите на другой диск
   - Продолжайте со следующими месяцами

2. **Используйте более низкую частоту:**
   - `--fps 0.5` вместо `--fps 1.0` (в 2 раза меньше кадров)
   - `--fps 0.33` для еще меньшего количества кадров

3. **Обрабатывайте только необходимые классы:**
   - Можно обработать только "Волк" или только "Лисица" для начала

## 🎯 Быстрое решение (минимальный вариант)

Если нужно быстро протестировать:

```bash
# Обработать только один месяц с низкой частотой
python scripts/extract_frames.py \
    --video_dir "data/videos/лисы-волки/01.2025" \
    --output_dir "data/frames/01.2025" \
    --fps 0.5 \
    --roi_output "data/roi/01.2025" \
    --preserve_structure \
    --process_images
```

## 📝 Что уже обработано

Судя по логам, успешно обработано:
- ~64 видео файла
- Созданы кадры из MP4, MOV, AVI файлов
- Кадры сохранены в `data/frames/` с сохранением структуры (месяц/класс)

**Не обработано:**
- Остальные ~142 видео (из-за нехватки места)
- Все .MOV файлы (требуют больше места)

## 🔄 Продолжение работы

После освобождения места:

1. **Проверьте, какие видео уже обработаны**
2. **Удалите или переместите обработанные кадры** (если нужно освободить место)
3. **Продолжите обработку оставшихся видео**

Или обработайте их на другом диске/месте с достаточным пространством.
